/**
 * English language layout wrapper.
 * SEO metadata is handled by each page's SEOHead component to ensure correct canonical URLs.
 */
const EnglishLayout = ({ children }: { children: React.ReactNode }) => children;

export default EnglishLayout;
